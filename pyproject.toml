[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lab-orchestrator"
version = "0.2.0"
description = "SSH and systemd based cluster script orchestration for lab machines"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "psutil>=5.9.0",
  "PyYAML>=6.0.0",
]

[project.scripts]
lab-orch = "lab_orchestrator.cli:main"
orch = "lab_orchestrator.cli:main"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["lab_orchestrator*"]
